# FLOW-1105 — Pi implementation report

Task:
FLOW-1105 — Tipizovani Plan Import API contract

Risk:
MEDIUM

Baseline:
5de0ee0f3f405fa3ca4081ed1bdb9e81ffd4c1fe

Branch:
task/FLOW-1105-plan-import-contract

## PRE-CHANGE FACTS

- stvarni endpoint path: `POST /projects/{project_id}/import-plan`
- stara endpoint signature:
  `def import_plan(project_id: str, body: dict, session: Session = Depends(get_session))`
  u `src/flowos/service/controllers/http/plan_progress.py:179`
- stvarni GUI payload: `{"markdown_text": markdown_text}` (potvrđeno u
  `src/flowos/gui/services/client.py:112-113`, docstring navodi "canonical
  PlanImportRequest polje")
- request/schema convention: Pydantic request modeli u
  `src/flowos/shared/contracts/plan_progress.py`; postojeći `PlanImportRequest`
  (`markdown_text: str`, `source_artifact_id: str | None = None`) već definisan,
  ali endpoint ga nije koristio
- relevantni test paths: `tests/integration/test_plan_progress_api.py`

Pretpostavke P1–P4 potvrđene read-only preflightom.

## GITNEXUS PRE

GITNEXUS_PRE = UNKNOWN (MCP alati nisu dostupni u pi okruženju; sibling worktree
index binding nepouzdan). Ručna kompenzacija: `rg` za `import-plan` / `import_plan`
/ `markdown_text` / `PlanImportRequest` u `src` i `tests`.

Rezultat blast radius analize: jedan endpoint, jedan HTTP pozivalac (GUI client,
payload već kompatibilan), postojeći test modul. Rizik MEDIUM zbog javnog API
contracta, ne zbog širine.

## TASK CONTRACT

agent_reports/FLOW-1105-task-contract.md

## CHANGED FILES

- src/flowos/service/controllers/http/plan_progress.py (endpoint + import)
- tests/integration/test_plan_progress_api.py (T1–T4 + missing `report_models` import)
- agent_reports/FLOW-1105-task-contract.md

## IMPLEMENTATION

- request model: `PlanImportRequest` (postojeći, `src/flowos/shared/contracts/plan_progress.py`)
- endpoint signature:
  `def import_plan(project_id: str, data: PlanImportRequest, session: Session = Depends(get_session))`
- downstream semantics: `PlanProgressService(session).import_plan(project_id, markdown)`
  — nepromijenjen poziv; `source_artifact_id` se i dalje NE prosljeđuje (zadržano
  postojeće ponašanje; njegovo prosljeđivanje bi promijenilo DB ponašanje i izlazi
  iz opsega FLOW-1105)
- response semantics: nepromijenjen (isti service return dict)
- business pravilo `markdown_text` prazan → HTTP 400 zadržano (nije transportna
  validacija); missing field i pogrešan tip sada standardni FastAPI 422

## CONTRACT TESTS

T1 (valid request):
- `test_t1_valid_request_imports_plan` — 200, items=1, phases=1, plan_id != "";
  dodatno dokaz da endpoint dobija isti markdown_text: Plan.title == "Test Plan"
  pročitan iz baze. PASS.

T2 (missing required field):
- `test_t2_missing_markdown_text_returns_422` — `{}` → 422; dokaz da downstream
  nije pozvan: `Plan` count za projekat == 0. PASS.

T3 (structurally wrong markdown_text type):
- `test_t3_non_string_markdown_text_returns_422` — `{"markdown_text": {"not": "a string"}}`
  → 422 (Pydantic 2.13.4, dict ne koercira u str). PASS.

T4 (response regression):
- `test_t4_response_shape_regression` — 200 sa tačno
  `{plan_id, phases, items, criteria, dependencies, unclear_count, unclear_sections}`. PASS.

## ADVERSARIAL PROOF

Mutation:
raw dict boundary restored temporarily (endpoint vraćen na `body: dict` +
`body.get("markdown_text", "")`).

Literal FAIL:
```
tests/integration/test_plan_progress_api.py::TestPlanImportContract::test_t2_missing_markdown_text_returns_422
FAILED
    def test_t2_missing_markdown_text_returns_422(self, client, project_id, app):
        resp = client.post(f"/projects/{project_id}/import-plan", json={})
>       assert resp.status_code == 422
E       assert 400 == 422
E        +  where 400 = <Response [400 Bad Request]>.status_code
1 failed, 1 warning in 1.56s
```

Restored typed contract.

Literal PASS:
```
tests/integration/test_plan_progress_api.py::TestPlanImportContract::test_t2_missing_markdown_text_returns_422
1 passed, 1 warning in 1.41s
```

## TARGETED TESTS

command:
```
python -m pytest tests/integration/test_plan_progress_api.py -q
```
literal output:
```
23 passed, 1 warning in 10.49s
```
(command takođe pokriva nova 4 contract testa; `TestPlanImportContract` samostalno:
`4 passed`)

## RUFF

```
python -m ruff check src/flowos/service/controllers/http/plan_progress.py tests/integration/test_plan_progress_api.py
All checks passed!

python -m ruff format --check src/flowos/service/controllers/http/plan_progress.py tests/integration/test_plan_progress_api.py
2 files already formatted
```

## ARCHITECTURE GUARD

```
python scripts/guard_architecture.py
[PASS] Arhitektura cista — nema zabranjenih importa.
```

## FULL VERIFY

command:
```
python scripts/verify.py
```
literal output (sažetak):
```
================= 566 passed, 1 warning in 146.57s (0:02:26) ==================
  [PASS] 1. Ruff format check
  [PASS] 2. Ruff lint
  [PASS] 3. mypy
  [PASS] 4. Architecture guard
  [PASS] 5. Architecture boundaries
  [PASS] 6. Unit tests
  [PASS] 7. Migrations check
  [PASS] 8. Alembic round-trip
  Prošlo: 8/8
[PASS] VERIFIKACIJA PROŠLA
```

## GITNEXUS POST

GITNEXUS_POST = UNKNOWN (isti razlog kao PRE). Ručni caller scan potvrdio:
route URL nepromijenjen (`POST /projects/{project_id}/import-plan`); GUI payload
`markdown_text` i dalje odgovara `PlanImportRequest.markdown_text`; response
contract nepromijenjen; nema neočekivanih caller promjena.

## MANUAL CALLER CHECK

- `src/flowos/gui/services/client.py:112` — jedini produkcijski HTTP caller,
  šalje `{"markdown_text": ...}`, kompatibilan.
- `tests/gui/test_gui_api_controller_methods.py` — GUI test mock-uje klijenta,
  ne pogađa endpoint; nepromijenjen.
- Endpoint `import_plan` definisan samo u `src/flowos/service/controllers/http/plan_progress.py`;
  nema drugih referenci.

## OUT_OF_SCOPE_FINDINGS

NONE. (Napomena, ne finding: u `tests/integration/test_plan_progress_api.py` je
dodan `import flowos.service.services.infrastructure.persistence.report_models  # noqa: F401`
— postojeći repo-wide pattern, neophodan da se test modul izolovano izvrši jer
SQLAlchemy mapper inicijalizacija zahteva registrovan `AgentReportBindingLink`.
Bez toga izolovano pokretanje test modula puca sa pre-existing
`InvalidRequestError`; import je unutar allowed_paths test fajla.)

## NOT VERIFIED

- Nezavisni review (TBD) — implementer ≠ reviewer.
- Human Owner / glavni arhitekta potvrda remote stanja.
- Ponašanje `source_artifact_id` prosljeđivanja (namjerno van opsega).

## IMPLEMENTATION COMMIT

39bc012b23f804c276356cd407feece6b7015f72

## REPORT COMMIT

(vidi naredni commit — ovaj fajl)

## PUSH TARGET

origin/task/FLOW-1105-plan-import-contract

## ODSTUPANJA

NONE
